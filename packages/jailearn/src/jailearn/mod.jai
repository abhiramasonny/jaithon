#: Classical machine learning — this language's answer to scikit-learn.
#:
#: A placeholder while the package is being written. The manifest and the
#: workspace entry exist first so that several authors can add modules beside
#: each other without every one of them editing the same two shared files.
pub let VERSION = "0.1.0"
